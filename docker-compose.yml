services:
  voicebrain:
    build: .
    env_file: .env
    environment:
      # defaults to the lite profile, override in .env
      WHISPER_MODEL: ${WHISPER_MODEL:-small}
      LLM_MODEL_PATH: ${LLM_MODEL_PATH:-models/Qwen2.5-3B-Instruct-Q4_K_M.gguf}
    volumes:
      - ./models:/app/models
      - ./data:/app/data
      - hf-cache:/root/.cache/huggingface
    restart: unless-stopped

volumes:
  hf-cache:
